# Copyright 2026 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

include "backend.conf"

# This is the main configuration file for the application.
# ~~~~~

appName = individuals-insurance-policies-income-api

appUrl = "http://localhost:7804"

# These are in DES format so '2020' corresponds to the 2019-20 tax year.
minimumPermittedTaxYear = 2020
ukDividendsMinimumTaxYear = 2018

# Session configuration
# ~~~~~
application.session.httpOnly = false

application.session.secure = false

# The application languages
# ~~~~~
play.i18n.langs = ["en"]

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# !!!WARNING!!! DO NOT CHANGE THIS ROUTER
play.http.router = prod.Routes

# Primary entry point for all HTTP requests on Play applications
play.http.requestHandler = "api.routing.VersionRoutingRequestHandler"

# Json error handler
play.http.errorHandler = "api.utils.ErrorHandler"

# Play Modules
# ~~~~
# Additional play modules can be added here
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.HttpClientV2Module"
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.AuthModule"
play.modules.enabled += "config.InsurancePlayModule"

# Api related config
api {
  # The status of the version of the API for the API Platform.
  controlled-access.enabled = false
  2.0 {
    status = "BETA"
    endpoints {
        enabled = true
        api-released-in-production = true
        allow-request-cannot-be-fulfilled-header = true
    }
  }

   confidence-level-check {
     confidence-level = 250
     definition.enabled = true
     auth-validation.enabled = true
   }

  supporting-agent-endpoints {
    retrieve-insurance-policies = false
    delete-insurance-policies   = false
    amend-insurance-policies    = false
  }

  # The context which the API will have via the API Platform http://API_GATEWAY/{api.gateway.context}/
  gateway.context = "individuals/insurance-policies-income"
}

bootstrap.http.headersAllowlist = [ "Accept", "Gov-Test-Scenario", "Content-Type", "Location", "X-Request-Timestamp", "X-Session-Id" ]

internalServiceHostPatterns = [ "localhost" ]

# Metrics plugin settings - graphite reporting is configured on a per env basis
metrics {
  enabled = true
}

# Microservice specific config

auditing {
  enabled = true
}

feature-switch {

  supporting-agents-access-control {
     enabled = true
     released-in-production = false
  }
}

microservice {

  services {
    auth {
      host = localhost
      port = 8500
    }

     mtd-id-lookup {
       host = localhost
       port = 9769
     }

    ifs {
      host = 127.0.0.1
      port = 9772
      env = Prod
      token = ABCD1234
      environmentHeaders = ["Accept", "Gov-Test-Scenario", "Content-Type", "Location", "X-Request-Timestamp", "X-Session-Id", "X-Request-Id"]
    }

    api1661 {
      host = 127.0.0.1
      port = 9772
      env = Prod
      token = ABCD1234
      environmentHeaders = ["Accept", "Gov-Test-Scenario", "Content-Type", "Location", "X-Request-Timestamp", "X-Session-Id", "X-Request-Id"]
    }
  }
}
